# redmine
Redmine

The script sets up a Redmine server running on Apache with a MySQL DB on a Debian system.
Once downloaded, the script only has to be run:

./redmine_main.sh

Once you got the server up and running connect to http://<server-ip-address> with:

username: admin
password: admin

